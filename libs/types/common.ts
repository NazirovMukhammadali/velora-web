/** Project-wide loose alias used for untyped event/handler params. */
export type T = any;

export interface MeLiked {
	memberId: string;
	likeRefId: string;
	myFavorite: boolean;
}

export interface TotalCounter {
	total: number;
}
